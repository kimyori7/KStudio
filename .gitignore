# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# IDE
.vscode/
.idea/

# Build artifacts
build/
dist/
# *.spec  ← KStudio.spec 은 프로젝트 빌드 레시피로 추적

# OS
Thumbs.db
.DS_Store

# Logs and temp recordings
logs/
*.log
tmp/
recordings/
/Video/

# Recording output files
*.mp4
*.mkv
*.webm
*.gif
*.aac
*.raw
*.wav

# Bundled ffmpeg binaries (too large for git; install via PATH or bin/)
bin/

# 개발 과정 문서는 저장소에 넣지 않는다 (로컬 보관).
# 기획서·계획서에는 내부 검증 기록과 업무 맥락이 그대로 남는다.
# 이 규칙이 없으면 새로 클론한 뒤 `git add .` 한 번으로 전부 다시 올라간다.
docs/superpowers/
.superpowers/
/CLAUDE.md

# Diagnostic PNG outputs (regenerable by scripts/diagnose_*.py — tools tracked, outputs not)
/diag_*.png
/diag_crop_magnifier/
/diag_diff_colors/
scripts/_diag_out/


# Claude Code local settings
.claude/
